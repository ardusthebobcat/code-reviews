import { eoaDisplay } from '../../../helpers/eoa-display';
import { module, test } from 'qunit';

module('Unit | Helper | eoa display');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eoaDisplay(42);
  assert.ok(result);
});
