import { docRating } from '../../../helpers/doc-rating';
import { module, test } from 'qunit';

module('Unit | Helper | doc rating');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = docRating(42);
  assert.ok(result);
});
