import { username } from '../../../helpers/username';
import { module, test } from 'qunit';

module('Unit | Helper | username');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = username(42);
  assert.ok(result);
});
