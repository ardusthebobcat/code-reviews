import { scoreRating } from '../../../helpers/score-rating';
import { module, test } from 'qunit';

module('Unit | Helper | score rating');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = scoreRating(42);
  assert.ok(result);
});
